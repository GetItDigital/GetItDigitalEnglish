% ET3 Übung 7.4 von Prof. Judith Ackers
% Uq über H-Brücke mit Schalter im Mittelpunkt. 
% R1 (oben links), R2 (oben rechts)
% C1 (unten links), R3 (unten rechts)
\begin{tikzpicture}
    %draw circuit
    \draw(0,0)
    to[short](2,0)
    to[short](4,0);
    \draw(0,0)
    to[V, name=V, v_=$U_q$,!v](0,-4)
    to[short](4,-4);
    \draw(2,0)
    to[R, l=$R_1$, *-*](2,-2)
    to[C, l=$C_1$, -*](2,-4);
    \draw(4,0)
    to[R, l=$R_2$, -*](4,-2)
    to[R, l=$R_3$, -*](4,-4);
    \draw(2,-2)
    to[short, -o](2.25,-2);
    \draw(4,-2)
    to[short, -o](3.75,-2);

    \draw(2.3,-2) to[spst](3.7,-2);

    %draw Spannungspfeil
    \varronly{V};
\end{tikzpicture}