\subsection{Complex AC calculation 3}
\Aufgabe{

    The following electrical network is to be analysed as an example of an equivalent circuit diagram of a lithium-ion cell with regard to its
    complex quantities. The equivalent circuit diagram consists of a series connection of a coil $L$, the 
    series resistance $R_\mathrm{S}$ and the parallel connection of the parallel resistance $R_\mathrm{P}$ and the parallel
    connected capacitor $C_\mathrm{P}$. The calculation of the complex currents and voltages of all components should be carried out in 
    the following order:
    
    \begin{itemize}
        \item[\bf a)] Magnitude of the complex impedances of all components and the total complex impedance
        \item[\bf b)] Complex currents through the components
        \item[\bf c)] Complex voltages across the components

    \end{itemize}
    
    \begin{figure}[H]
        \renewcommand{\figurename}{Figure}
        \centering
        \resizebox{0.7\textwidth}{!}{
            \input{Tikz/src/Aufgaben/Komplexe_Wechselstromrechnung3}
        }
    \end{figure}
}


\Loesung{
    \begin{itemize}

        \item[\bf a)]
              \begin{eq}
                  X_\mathrm{L} = j \omega \cdot L = j \cdot 2\pi \cdot 800\ Hz \cdot 900\ nH  \nonumber
              \end{eq}
              \begin{eq}
                  X_\mathrm{Rs} = R_\mathrm{S} = 20\ m\Omega
              \end{eq}
              
        \item[\bf b)]
        
        \item[\bf c)]  
        
    \end{itemize}
    
}