\begin{tikzpicture}
 \draw
    (0,2) to[V, name=V1] (0,0);
    % Widerstand
    \draw
    (0,2) to[R=$10 \,\mathrm{\Omega}$, i,name=i1, -*] (4,2) to[short, i,name=i2] (4.9,2);
    \draw (4,2)to[short, i,name=i3] (4,1.6);

    \draw (0,0) to[short, -o](5,0) -- (7,0);
  
    % LED rechts
    \draw
    (4,0) to[zDo=$D_\mathrm{Z}$, v< ,name=v2, *-] (4,2) ;

    \draw(4,2) to[short, -o](5,2)  --(7,2);
    \draw(7,2) to[R=$R_\mathrm{L}$] (7,0);

    \draw (5,2) to[open,v>, name=ua] (5,0);
    \varrmore{V1}{$20\,\mathrm{V} \pm 10 \, \%$};
    \iarrmore{i1}{${I}$};
    \iarrmore{i2}{$I_\mathrm{L}$};
    \iarrmore{i3}{$I_\mathrm{D}$};
    \varrmore{v2}{$8\,\mathrm{V}$};

\end{tikzpicture}
