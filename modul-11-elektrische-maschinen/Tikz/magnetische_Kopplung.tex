\def\vx{2.0}%Verschiebung X
\def\vy{1.0}%Verschiebung Y
\def\shiftx{20.0}%Verschiebung Sekundärspule
\begin{circuitikz}[thick]
    % Magnetfeld unten
    \onslide<2->{
        \draw [yshift = -14, line width = 1.5, red, ->] (7,3) arc (190:360:3) node[above]{$\varPhi$};
    }
    \onslide<4>{
        \draw [yshift = -14, xscale = 0.55, line width = 1.5, red, dashed, opacity = 0.4] (12.75,3) arc (-170:180:3);
        \draw [yshift = -14, xscale = 0.45, line width = 1.5, red, dashed, opacity = 0.2] (15.6,3) arc (-170:180:3);
    }
    % Primärspule
    \draw [->] (0,0) to[short, -] node[above]{$\underline{I}_1$} ++(\vx,\vy);
    \draw (0,0) to[sV_=$\underline{U}_1$] ++(\vx,-\vy);
    \draw (\vx,-\vy) to[short, -] ++(\vx,\vy);
    \draw (\vx,\vy) .. controls ++(\vx,\vy) and ++(-3,1) .. ++(6,3)
    .. controls ++(1.5,-.5) and ++(0,0) .. ++(1.5,-1.5)
    .. controls ++(0,-2) and ++(\vx,\vy) .. (\vx*2,0);
    % Sekundärspule
    \onslide<3->
    {
        \draw [->] (\shiftx,0) to[short, -] node[above]{$\underline{I}_2$} ++(-\vx,\vy);
        \draw (\shiftx,0) to[sV=$\underline{U}_2$] ++(-\vx,-\vy);
        \draw (\shiftx-\vx,-\vy) to[short, -] ++(-\vx,\vy);
        \draw (\shiftx-\vx,\vy) .. controls ++(-\vx,\vy) and ++(3,1) .. ++(-6,3)
        .. controls ++(-1.5,-.5) and ++(0,0) .. ++(-1.5,-1.5)
        .. controls ++(0,-2) and ++(-\vx,\vy) .. (\shiftx-\vx*2,0);
    }
    % Magnetfeld oben
    \onslide<2->{
        \draw [yshift = 2, line width = 1.5, red, opacity = 1, <-] (7,3) node[below]{$\varPhi$} arc (180:10:3) ;
    }
\end{circuitikz}