\subsection{Three-phase current 2}
\Aufgabe{
    It is a symmetrical three-phase system with the following voltages:
    
    $\underline{U}_1=230~V$, $\underline{U}_2=230~V\cdot \underline{a}^2$, $\underline{U}_3=230~V\cdot \underline{a}$
    
    The impedances all have a value of $Z=10\Omega $
    
    The following applies to all parts of the exercise: Solve the exercises with the least possible effort!
    
    \begin{itemize}
        \item [\bf a)] Draw the three-phase and single-phase ESBs in star and delta connections and label the ECDs correctly!
        \item [\bf b)] Calculate the magnitude of the consumer voltages for both the star and delta connections!
        \item [\bf c)] Calculate the complex consumer voltages in the delta connection!
        \item [\bf d)] Draw the phasor diagrams of the consumer voltages for the star and delta connections! 
        \item [\bf e)] Calculate the complex load currents for the star connection and the magnitude of the load currents for the delta connection!
        \item [\bf f)] Calculate the apparent power for the star and delta connections!
    \end{itemize}
}


\Loesung{
    \begin{itemize}

        \item[\bf a)]
        
              ECDs of the star connection:\\
              \resizebox{0.7\textwidth}{!}{%
                  \input{Tikz/src/Aufgaben/Loesung_Drehstrom2_a1}
              }
              
              \input{Tikz/src/Aufgaben/Loesung_Drehstrom2_a2}
              
              ECDs of the triangular circuit:\\
              
              \resizebox{0.7\textwidth}{!}{%
                  \input{Tikz/src/Aufgaben/Loesung_Drehstrom2_a3}
              }
              
              \input{Tikz/src/Aufgaben/Loesung_Drehstrom2_a4}
              
        \item[\bf b)]
            The amount of consumer voltage in a star connection is equal to the source voltage: 230 V.
            The amount of consumer voltage in a delta connection is $\sqrt{3}$ times greater than in a star connection: $230 V\cdot \sqrt{3}=398.37 V\approx 400 V$.              
        \item[\bf c)]
        
              The complex consumer voltage in the delta connection is calculated from the differences in the star voltages.
              
              \begin{eqa}
                  \underline{U}_\mathrm{L1L2}&=\underline{U}_\mathrm{L1}-\underline{U}_\mathrm{L2}=230 V-230 V\cdot \underline{a}^2 \notag \\
                  &=230 V(1-(-\frac{1}{2}-j\frac{\sqrt{3}}{2}))=\sqrt{3}\cdot 230 V(\frac{3}{2}+j\frac{1}{2}) \notag \\
                  &=\sqrt{3}\cdot 230 V\cdot e^{j\cdot 30^\circ} \notag
              \end{eqa}
              \begin{eqa}
                  \underline{U}_\mathrm{L2L3}&=\underline{U}_\mathrm{L2}-\underline{U}_\mathrm{L3}=230 V\cdot \underline{a}^2-230 V\cdot \underline{a} \notag \\
                  &=230 V(-\frac{1}{2}-j\frac{\sqrt{3}}{2}-(-\frac{1}{2}+j\frac{\sqrt{3}}{2}))=\sqrt{3}\cdot 230 V(-j) \notag \\
                  &=\sqrt{3}\cdot 230 V\cdot e^{-j\cdot 90^\circ} \notag
              \end{eqa}
              \begin{eqa}
                  \underline{U}_\mathrm{L3L1}&=\underline{U}_\mathrm{L3}-\underline{U}_\mathrm{L1}=230 V\cdot \underline{a}-230 V \notag \\
                  &=230 V(-\frac{1}{2}+j\frac{\sqrt{3}}{2}-1)=\sqrt{3}\cdot 230 V(-\frac{\sqrt{3}}{2}+j\frac{1}{2}) \notag \\
                  &=\sqrt{3}\cdot 230 V\cdot e^{j\cdot 150^\circ} \nonumber
              \end{eqa}
              
        \item[\bf d)]
        
            Pointer diagrams of the consumer voltages for star and delta connections:\\
              
              \begin{figure}
                  \centering
                  \resizebox{0.6\textwidth}{!}{%
                      \input{Tikz/src/Aufgaben/Loesung_Drehstrom2_d_Zeigerfiagramm}
                  }
              \end{figure}
              
        \item[\bf e)]
        
              The current is calculated using Ohm's law. The following applies to star connections:
              
              \begin{eqa}
                  \underline{I}_1&=\frac{\underline{U}_\mathrm{L1}}{\underline{Z}}=\frac{230 V}{10\ohm}=23 A \notag \\
                  \underline{I}_2&=\frac{\underline{U}_\mathrm{L2}}{\underline{Z}}=\frac{230 V\cdot \underline{a}^2}{10\ohm}=23 A\cdot\underline{a}^2  \notag \\
                  \underline{I}_3&=\frac{\underline{U}_\mathrm{L3}}{\underline{Z}}=\frac{230 V\cdot \underline{a}}{10\ohm}=23 A\cdot\underline{a}   \notag
              \end{eqa}
              
              With delta current, a distinction is made between the phase current and the delta current, i.e. the current through the consumers.
              The current in the consumer phases can be calculated using the voltage across the consumers in the delta circuit:
              
              \begin{eqa}
                  I_\Dreieck&=\frac{U\Dreieck}{Z}=\frac{400V}{10\ohm}=40 A \notag
              \end{eqa}
              
              The current in the strand is again $\sqrt{3}$ greater than in the consumer strands:
              \begin{eqa}
                  I_\mathrm{str}=\sqrt{3}\cdot I_\Dreieck=\sqrt{3}\cdot 40 A=69,28 A \notag
              \end{eqa}
              
              This value corresponds to three times the value of the current in a star connection.
              
              
        \item[\bf f)]
        
              The general power equation is as follows:
              
              \begin{eqa}
                  S=\sqrt{3}\cdot U_\Dreieck \cdot I_\mathrm{str} \notag
              \end{eqa}
              
              The current required depends on the circuit in question.
              As calculated in task 5, the phase current in the delta circuit is three times greater than in the star circuit.
              
              The following applies to star connections:
              \begin{eqa}
                  S=\sqrt{3}\cdot U\cdot I=\sqrt{3}\cdot 400 V \cdot 23 A=15.934 VA \notag
              \end{eqa}
              
              And for the delta connection, the following applies:
              \begin{eqa}
                  S=\sqrt{3}\cdot U\cdot I=\sqrt{3}\cdot 400 V \cdot 3\cdot 23 A=47.804 VA \notag
              \end{eqa}
              
    \end{itemize}
    
}